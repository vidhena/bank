pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
                bat 'javac *.java'
            }
        }

        stage('Run') {
            steps {
                bat 'java Main'
            }
        }
    }

    post {

        success {

            archiveArtifacts '*.class'

            echo 'Build Successful and Artifacts Stored'
        }

        failure {
            echo 'Build Failed'
        }
    }
}
